$schema: https://azuremlschemas.azureedge.net/latest/commandComponent.schema.json
name: promote_model
display_name: Promote Model
version: 1
type: command

inputs:
  model_input:
    type: uri_folder

outputs:
  promoted_model:
    type: uri_folder

environment:
  image: transformerloadacr.azurecr.io/transformer-load-image:latest

code: .
command: >-
  python promote.py