$schema: https://azuremlschemas.azureedge.net/latest/commandComponent.schema.json
name: promote_transformer_load_model
display_name: Promote Transformer Load Model
version: 1
type: command

inputs:
  model_input:
    type: mlflow_model
    mode: ro_mount

outputs:
  promoted_model:
    type: mlflow_model
    mode: rw_mount

environment:
  image: transformerloadacr.azurecr.io/transformer-load-image:latest

code: .
command: >-
  python -u promote.py